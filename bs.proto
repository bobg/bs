syntax = "proto3";
package bs;

option go_package = ".;bs";

message TreeNode {
  // Refs of other TreeNodes.
  repeated bytes nodes = 1;

  // Refs of leaf blobs.
  repeated bytes leaves = 2;

  // Number of bytes represented by this node.
  uint64 size = 3;
}
