syntax = "proto3";
package dsync;

option go_package = ".;dsync";

message Dirent {
  string name = 1;
  uint32 mode = 2;
}

message Dir {
  repeated Dirent entries = 1; // Keep this sorted by name.
}
